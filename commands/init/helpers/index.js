const queryDecoder = require('./queryDecoder');
const requestDecoder = require('./requestDecoder');
const serveStatic = require('./serveStatic');
const sendRequest = require('./sendRequest');

module.exports = {
	queryDecoder,
	requestDecoder,
	serveStatic,
	sendRequest
}