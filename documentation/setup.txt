sets up the cli by adding two files (for Windows and Linux) in a folder from the environment variable 'path' or in a node_modules folder

flags:
-l/--local      instead of globally, the cli is installed in a node_modules folder